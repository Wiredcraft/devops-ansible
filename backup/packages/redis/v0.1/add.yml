---
- name: Ensure redis is installed
  apt: pkg=redis-server state=present
  notify: enable redis

# # We deploy the deault config file on install; but we shouldn't
# # be updating it later on
# - name: Ensure nginx default config is installed
#   template: 
#     src=DEVOPS_PACKAGE/templates/nginx.conf.j2
#     dest=/etc/nginx/nginx.conf
#     owner=root
#     group=root
#     mode=0644

# Ensure running
- include: DEVOPS_PACKAGE/tasks/start.yml