---
- name: Delete Rackspace server
  local_action:
    module: rax
    region: "{{ location }}"
    api_key: "{{ api_key }}"
    username: "{{ api_user }}"
    name: "{{ repo }}-{{ node_id }}"
    state: absent