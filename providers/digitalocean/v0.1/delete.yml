---
- name: Delete Digital Ocean server
  action:
    module: digital_ocean
    client_id: ${client_id}
    api_key: ${api_key}
    command: droplet
    state: absent
    id: ${provider_id}
  delegate_to: 127.0.0.1
