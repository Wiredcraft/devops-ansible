---
- hosts: ${server}
  vars_files:
    - ../../config/global.yml
  tasks:
    # Dependencies first
    # Self
    - include: tasks/main.yml
  handlers:
    # Dependencies first
    - include: ../apache/handlers/main.yml
    - include: ../php5-fpm/handlers/main.yml
    # Self
    - include: handlers/main.yml