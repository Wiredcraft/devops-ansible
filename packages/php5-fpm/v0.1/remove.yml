---
- name: Ensure php5-fpm is absent
  apt: pkg=php5-fpm state=absent

# Later need to add the backup step; or rely on the task system 
# to schedule the backup task before.

- name: ensure php5-fpm configuration is absent
  file: 
    state=absent
    recurse=yes
    path=$item
  with_items:
    - /etc/php5/fpm

- name: ensure php5-fpm monitoring is absent
  file: 
    state=absent
    path=/etc/collectd/plugins.d/php5-fpm
  notify: restart collectd

