{% set mysql = mysql | default({}) %}
LoadPlugin mysql

<Plugin "mysql">
  <Database "{{ inventory_hostname }}">
    Host "localhost"
    User "monitoring"
    Password "{{ item }}"
    Port "{{ mysql.port | default('3306') }}"
    MasterStats true
  </Database>
</Plugin>