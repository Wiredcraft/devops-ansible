[{
    "id": "git_clone",
    "label": "Clone git repository",
    "options": [{
            "id": "git_repo_address",
            "label": "Git repository address",
            "required": true,
            "default": "_"
        },
        {
            "id": "git_repo_dest",
            "label": "Directory where repo is cloned",
            "required": false,
            "default": "/opt/devops"
        }
    ]
},
{
    "id": "etcd_fetch_and_set",
    "label": "Fetch value from etcd server and set it up as env var",
    "options": [{
            "id": "etcd_host",
            "label": "Host name or ip for etcd server",
            "required": true,
            "default": "_"
        },
        {
            "id": "etcd_key",
            "label": "Key name (without leading slash) of an etcd item",
            "required": true,
            "default": "_"
        }
    ]
}]